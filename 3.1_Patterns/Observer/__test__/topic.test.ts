// const { Topic } = require("../app/topic")
// const { User } = require("../app/topic")

// test("does not add twice the same User", () => {
//     const topic = new Topic("Test Topic")
//     const user = new User("Test User")
//     topic.subscribeUser(user)
//     topic.subscribeUser(user)
//     expect(topic.getProperties().user.length).toBe(1)
// })