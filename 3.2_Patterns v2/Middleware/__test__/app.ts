// This file it has been created to avoid to launch the server everytime that the test is executed
// Those 3 functions are exactly the same that the ones you will find on "../app/app.ts"

export function sum(a: number, b: number): number {
    return a + b
}

export function multiply(a: number, b: number): number {
    return a * b
}

export function divide(a: number, b: number): number {
    return a / b
}